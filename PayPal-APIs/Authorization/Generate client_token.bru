meta {
  name: Generate client_token
  type: http
  seq: 4
}

post {
  url: {{base_url}}/v1/identity/generate-token
  body: json
  auth: inherit
}

body:json {
  {
      "customer_id": "{{$timestamp}}"
  }
}

settings {
  encodeUrl: true
}
